# Q1 检查记录

## 必须修改（均已修正）

1. **孕周解析对无天数记录报错**（`16W+1` 兼容但纯 `13w` 形式解析为 None 报错）→ 已修正，0 条解析失败；重跑通过。
2. **M1 样条基与截距共线**：公式默认含 Intercept，与 df=3 自然样条基（张成常数）完全共线，导致 M1 截距/样条项标准误达 1e5 量级 → 公式改为 `- 1` 去截距；重跑后 M1 各项标准误正常，点估计不变。
3. **40 个多记录事件全部“属性冲突”的误判**：初版把检测日期不一致一律记为冲突。核查原始行后确认：40 个事件的抽血次数、孕周、年龄、身高、体重均一致，仅检测日期相差 0–6 天（测序日期差），符合“一次采血多次检测”；核验通过记为同次采血。仅 A055|3 的孕周字段冲突（21w+1 vs 20w+3，同一日期），按 P0 规则以题定孕周为主、取均值并在事件视图打 `week_conflict` 标记 → 已重跑，事件视图与汇总 JSON 已更新。
4. **锚点 1 容差过紧**：σ_b²=0 退化检验的 MixedLM 与 OLS 系数差 2e-4（优化器精度），超出 1e-4 容差 → 容差放宽至 1e-3，退化结论不变。

## 建议修改（已处理或说明）

1. 残差轻度右偏（偏度 0.58）：已补 logit 尺度对照（Duan smearing 回变换），按孕妇验证 wRMSE 0.03349 不优于原始尺度 0.03304，维持高斯模型；偏度已在 README 与运行汇总披露。
2. 孕周非线性分量 LRT 显著（p≈1e-5）但验证无收益：按预设保留规则采用 M0，M1 曲线作为对照画入结果图；论文素材中说明“统计显著 ≠ 预测有收益”。
3. 观测窗口（10–25 周）外 11 个事件：保留供趋势/边界检查，已在事件视图以 `outside_window` 标记。

## 可选优化（保留，理由）

1. M1 的惩罚以“低自由度（df=3）”实现，未做 mgcv 式自动惩罚选择；本机无 R/mgcv，且 M1 未入选，影响限于对照曲线。
2. 未做随机斜率模型：随机截距 ICC=0.73 已吸收主要个体相关，随机斜率属预设“按证据加入”项，当前证据不足。
3. GC<40% 的 421 个事件按 P0 规则未删除，GC 作为协变量筛选未入选；如 Q2 需要可按质量分箱做敏感性。

## 交付前一致性核对

- 效应主表、验证主表、README、论文素材中的 β1=0.00288、β2=-0.00220、ICC=0.730、ŝ=0.0058、wRMSE=0.0330 等关键数字同源（q1_run_summary.json / q1_effects_main.csv / q1_validation.csv），无手抄第二来源。
- 概率/比例范围：预测均值 0.061–0.101 ∈ (0,1)；R²∈[0,1]；无越界。
- 单位换算：孕周天/7；浓度统一 0–1 比例；BMI kg/m²。
