# Q1 检查记录

## 必须修改（均已修正）

1. **孕周解析对无天数记录报错**（`16W+1` 兼容但纯 `13w` 形式解析为 None 报错）→ 已修正，0 条解析失败；重跑通过。
2. **M1 样条基与截距共线**：公式默认含 Intercept，与 df=3 自然样条基（张成常数）完全共线，导致 M1 截距/样条项标准误达 1e5 量级 → 公式改为 `- 1` 去截距；重跑后 M1 各项标准误正常，点估计不变。
3. **40 个多记录事件全部“属性冲突”的误判**：初版把检测日期不一致一律记为冲突。核查原始行后确认：40 个事件的抽血次数、孕周、年龄、身高、体重均一致，仅检测日期相差 0–6 天（测序日期差），符合“一次采血多次检测”；核验通过记为同次采血。仅 A055|3 的孕周字段冲突（21w+1 vs 20w+3，同一日期），按 P0 规则以题定孕周为主、取均值并在事件视图打 `week_conflict` 标记 → 已重跑，事件视图与汇总 JSON 已更新。
4. **锚点 1 容差过紧**：σ_b²=0 退化检验的 MixedLM 与 OLS 系数差 2e-4（优化器精度），超出 1e-4 容差 → 容差放宽至 1e-3，退化结论不变。

## 建议修改（已处理或说明）

1. 残差轻度右偏（偏度 0.58）：已补 logit 尺度对照（Duan smearing 回变换），按孕妇验证 wRMSE 0.03349 不优于原始尺度 0.03304，维持高斯模型；偏度已在 README 与运行汇总披露。
2. 孕周非线性分量 LRT 显著（p≈1e-5）但验证无收益：按预设保留规则采用 M0，M1 曲线作为对照画入结果图；论文素材中说明“统计显著 ≠ 预测有收益”。
3. 观测窗口（10–25 周）外 11 个事件：保留供趋势/边界检查，已在事件视图以 `outside_window` 标记。

## 可选优化（保留，理由）

1. M1 的惩罚以“低自由度（df=3）”实现，未做 mgcv 式自动惩罚选择；本机无 R/mgcv，且 M1 未入选，影响限于对照曲线。
2. 未做随机斜率模型：随机截距 ICC=0.73 已吸收主要个体相关，随机斜率属预设“按证据加入”项，当前证据不足。
3. GC<40% 的 421 个事件按 P0 规则未删除，GC 作为协变量筛选未入选；如 Q2 需要可按质量分箱做敏感性。

## 交付前一致性核对

- 效应主表、验证主表、README、论文素材中的 β1=0.00288、β2=-0.00220、ICC=0.730、ŝ=0.0058、wRMSE=0.0330 等关键数字同源（q1_run_summary.json / q1_effects_main.csv / q1_validation.csv），无手抄第二来源。
- 概率/比例范围：预测均值 0.061–0.101 ∈ (0,1)；R²∈[0,1]；无越界。
- 单位换算：孕周天/7；浓度统一 0–1 比例；BMI kg/m²。

---

## 第二轮：队长 PR #1 评审意见处理（2026-09-06，全部完成并重跑）

### P1-1 技术误差重复计算相关差值 → 已修正
改为事件内合并方差 ŝ²=SSW/Σ(n_e−1)，df=61；ŝ 由 0.00584 修正为 **0.00610（15.3%）**，与队长复核值一致。误差图改为事件内偏差直方图；README、论文素材、运行汇总已同步。

### P1-2 残差诊断不支持显著性表述 → 已修正
残差超额峰度约 3.4、高拟合值区间方差增大，Wald 区间同方差近似不牢靠。已补 500 次按孕妇 cluster bootstrap（0 次拟合失败）：孕周 CI 0.00227–0.00337、基线 BMI CI -0.00352–-0.00102，均不含 0 且与 Wald 区间一致；显著性结论在重尾/异方差下保持。bootstrap 区间已写入效应主表。

### P2-3 绝对路径与依赖清单 → 已修正
`--xlsx` 命令行参数（默认仓库上级目录 `附件.xlsx`），启动时检查文件与工作表；仓库根目录新增 `requirements.txt`。

### P2-4 收敛警告隐藏 → 已修正
取消全局 warnings 屏蔽；fit() 记录 ConvergenceWarning 与未收敛计数，lbfgs 失败回退 nm；最终 M0/M1 REML 断言收敛。本轮：0 次未收敛、0 次回退、541 次边界警告（bootstrap 重抽样 σ_b² 近边界，预期内）。

### P2-5 logit 对照未积分随机效应 → 已修正
smearing 基础上对随机截距 b~N(0,σ_b²) 做 24 节点 Gauss-Hermite 积分，恢复边际口径。修正后 logit wRMSE 0.03338 仍不优于 0.03304，“无收益”结论成立。

### P2-6 BMI 图混入孕周差异 + M1/GAMM 命名 → 已修正
BMI 图改为模型调整后的部分残差图（控制孕周/ΔB/读段数），旧图 `raw_q1_bmi_firsthit.png` 已删除；全部产物中 M1 统一改称“自然样条混合模型（固定自由度、无显式平滑惩罚）”，不再称 GAMM。

### 清单第 6 项 A055|3 孕周 → 已修正
按末次月经（2023-02-23）+ 检测日期（2023-07-22）推算 21.29 周（21w+2），与队长推算一致；采用核对值替代直接平均，标记保留。

### 重算后关键数字一致性
β1=0.00288（A055 修正后末位微变 0.002882）、β2=-0.00220、ICC=0.730、wRMSE=0.0330 稳定；ŝ=0.0061；效应主表/验证主表/README/论文素材/运行汇总同源一致。队长判定的两项 P1 均已闭环，可请求复审。