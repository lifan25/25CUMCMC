# 建模练习工作流

## 总流程

`P0 全题规划 → Q1 → Q2 → Q3 → Q4 → 全题收束 → 论文`

P0 已完成；当前进度与正式结果以 `PROJECT_STATUS.md` 为准。每次从状态记录继续，不重复初始化或额外设置审核门禁。

## 每问闭环

每个 `Qn` 后续按以下顺序展开：

1. `01_题意与建模`：目标、变量、假设、约束、公式和可执行算法；
2. `02_编程`：最小示例与正式求解；
3. `03_可视化`：只保留能支持结论的图；
4. `04_结果`：正式结果主表、关键数字与解释；
5. `05_审查与修改`：按必须修改、建议修改、可选优化记录；
6. `06_论文素材`：形成可直接写入论文的连续材料。

目录只在有真实内容时创建，不为补齐形式生成空文件。

## 数据与数字口径

- 原始 PDF 与 Excel 不覆盖、不改写。
- 孕周统一为十进制周：`周 + 天/7`。
- 浓度与比例在计算中使用小数，在解释中可换算为百分比。
- 重复检测按“孕妇代码—抽血次数—孕周”识别；训练、验证或重抽样必须以孕妇为分组单位。
- 各问完成时在 `README_Qn.md` 指定正式结果主表；P0、图表和论文只引用该表。

## 模型升级原则

先做数据质量、描述规律、量纲与边界检查。基线模型若能可靠回答题目则保留；只有重复测量、删失、非线性、类别不平衡或训练外表现明确暴露不足时才升级，并用真实验证结果说明收益。
